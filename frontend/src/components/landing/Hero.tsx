"use client";
import Link from "next/link";
import { useTokenCheck } from "@/lib/hooks/useTokenCheck";
import { Button } from "@/components/ui/button";

export function Hero() {
  const { hasToken, loading } = useTokenCheck();

  return (
    <section className="relative min-h-screen flex items-center justify-center bg-surface-0 overflow-hidden">
      {/* Hero background image */}
      <div
        className="absolute inset-0 z-0 bg-cover bg-center bg-no-repeat"
        style={{ backgroundImage: "url(/hero-bg.png)" }}
        aria-hidden="true"
      />

      {/* Gradient overlay to blend image */}
      <div
        className="absolute inset-0 z-5 pointer-events-none"
        aria-hidden="true"
        style={{
          background:
            "linear-gradient(135deg, hsl(var(--surface-0) / 0.88) 0%, hsl(var(--surface-0) / 0.70) 30%, hsl(var(--surface-0) / 0.35) 55%, transparent 85%)",
        }}
      />

      {/* Hero section bottom shadow/depth */}
      <div
        className="absolute bottom-0 left-0 right-0 z-10 pointer-events-none"
        aria-hidden="true"
        style={{
          height: "120px",
          boxShadow: "inset 0 -40px 40px hsl(var(--surface-1) / 0.8), 0 4px 20px rgba(0,0,0,0.3)",
        }}
      />

      {/* Atmospheric background layers */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none z-10" aria-hidden="true">
        {/* Corner shine effects */}
        <div className="corner-shine-tl" />
        <div className="corner-shine-tr" />
        <div className="corner-shine-bl" />
        <div className="corner-shine-br" />
        <div className="hero-spotlight" />

        {/* Animated gradient mesh orbs */}
        <div className="hero-orb hero-orb-1" />
        <div className="hero-orb hero-orb-2" />
        <div className="hero-orb hero-orb-3" />
        <div className="hero-orb hero-orb-4" />
      </div>

      {/* Content */}
      <div className="container mx-auto px-4 relative z-20 flex flex-col items-center text-center py-32">
        {/* Badge */}
        <div className="animate-fade-in-up mb-8 inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-border-standard bg-surface-2/60 backdrop-blur-sm">
          <span className="badge-linear-success" />
          <span className="text-xs font-medium text-text-secondary tracking-wide">AI-Powered GitHub Intelligence</span>
        </div>

        {/* Headline */}
        <h1 className="animate-fade-in-up animate-delay-100 text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-medium tracking-tight text-balance text-text-primary font-feature-settings-cv01-ss03" style={{ letterSpacing: "-0.03em", textShadow: "0 2px 12px rgba(0,0,0,0.4)" }}>
          RepoNarrate
        </h1>

        {/* Subtitle */}
        <p className="animate-fade-in-up animate-delay-200 mx-auto mt-6 max-w-2xl text-lg md:text-xl text-text-secondary text-pretty leading-relaxed" style={{ textShadow: "0 1px 6px rgba(0,0,0,0.3)" }}>
          Connect your GitHub repo. Get human-readable changelogs, categorized commits, and semantic search — all generated by AI.
        </p>

        {/* CTA Buttons */}
        <div className="animate-fade-in-up animate-delay-300 mt-10 flex flex-col sm:flex-row gap-4 items-center">
          {loading ? (
            <div className="h-12 w-48 bg-muted rounded-md animate-pulse" />
          ) : hasToken ? (
            <Link href="/dashboard">
              <Button size="lg" className="btn-linear-primary px-8 py-3 text-base font-medium shadow-lg shadow-brand-indigo/20">
                Go to Dashboard
              </Button>
            </Link>
          ) : (
            <a href={`${process.env.NEXT_PUBLIC_API_URL}/auth/github`}>
              <Button size="lg" className="btn-linear-primary px-8 py-3 text-base font-medium shadow-lg shadow-brand-indigo/20 hover:shadow-brand-indigo/30 transition-all duration-200">
                Get Started Free
              </Button>
            </a>
          )}
          <Link href="#features">
            <Button variant="outline" size="lg" className="btn-linear-ghost px-6 py-3 text-base font-medium">
              Learn More
            </Button>
          </Link>
        </div>

        {/* Social proof */}
        <div className="animate-fade-in-up animate-delay-500 mt-16 flex flex-col items-center gap-3">
          <span className="text-xs text-text-tertiary uppercase tracking-widest">Trusted by developers worldwide</span>
          <div className="flex items-center gap-2">
            <div className="flex -space-x-2">
              <div className="h-7 w-7 rounded-full bg-gradient-to-tr from-brand-indigo to-brand-violet border-2 border-surface-0" />
              <div className="h-7 w-7 rounded-full bg-gradient-to-tr from-success-green to-success-emerald border-2 border-surface-0" />
              <div className="h-7 w-7 rounded-full bg-gradient-to-tr from-brand-hover to-brand-violet border-2 border-surface-0" />
            </div>
            <span className="text-xs text-text-quaternary">Join 2,000+ developers</span>
          </div>
        </div>
      </div>
    </section>
  );
}
